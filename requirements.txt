# Pinned for reproducible builds (Phase 1 hardening 2026-08-11).
# Update via a controlled PR after security review; never let Railway
# auto-resolve floating minors on a redeploy.

# Web framework + ASGI server
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Rate limiting
slowapi==0.1.9

# Adobe SDK — kept for USE_ADOBE=true opt-in fallback
pdfservices-sdk==4.2.0

# PDF → DOCX / XLSX converters (self-hosted, unlimited)
pdf2docx==0.5.8
pdfplumber==0.11.4
openpyxl==3.1.5
